<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Add Toy - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: #343a40;
            color: white;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }

        .form-label {
            font-weight: bold;
        }

        .form-control {
            border-radius: 5px;
        }

        .btn-submit {
            background-color: #343a40;
            color: white;
            border: none;
            border-radius: 5px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h4>Add Toy</h4>
                    </div>
                    <div class="card-body">
                        <form action="/admin/alltoys/add" method="POST">
                            <div class="mb-3">
                                <label for="bname" class="form-label">Name:</label>
                                <input type="text" id="bname" name="bname" class="form-control" required>
                            </div>
                            <div class="mb-3">
                                <label for="bcategory" class="form-label">Category:</label>
                                <input type="text" id="bcategory" name="bcategory" class="form-control" required>
                            </div>
                            <div class="mb-3">
                                <label for="bdetail" class="form-label">Detail:</label>
                                <input type="text" id="bdetail" name="bdetail" class="form-control" required>
                            </div>
                            <div class="mb-3">
                                <label for="bimage" class="form-label">Image:</label>
                                <input type="text" id="bimage" name="bimage" class="form-control" required>
                            </div>
                            <div class="mb-3">
                                <label for="bprice" class="form-label">Price:</label>
                                <input type="number" id="bprice" name="bprice" class="form-control" inputmode="numeric"
                                    required>
                            </div>
                            <div class="mb-3">
                                <label for="bquantity" class="form-label">Quantity:</label>
                                <input type="number" id="bquantity" name="bquantity" class="form-control"
                                    inputmode="numeric" required>
                            </div>
                            <label for="bgender" class="form-label">Gender:</label>
                            <select id="bgender" name="bgender" class="form-select" required>
                                <option value="male">Boy toy</option>
                                <option value="female">Girl Toy</option>
                            </select>

                            <button type="submit" class="btn btn-submit">Add Toy</button>
                            <a href="/admin/alltoys" class="btn btn-submit">Back to Admin Page</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>