<!-- update.hbs -->
<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Update Toy - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* ... Các tùy chỉnh CSS ... */
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h4>Update Toy</h4>
                    </div>
                    <div class="card-body">
                        <form action="/admin/alltoys/update/{{ toy._id }}" method="POST">
                            <div class="mb-3">
                                <label for="name" class="form-label">Name:</label>
                                <input type="text" id="name" name="name" class="form-control"
                                    value="{{#if toy.bname}}{{ toy.bname }}{{else}}{{ toy.gname }}{{/if}}" required>
                            </div>
                            <div class="mb-3">
                                <label for="category" class="form-label">Category:</label>
                                <input type="text" id="category" name="category" class="form-control"
                                    value="{{#if toy.bcategory}}{{ toy.bcategory }}{{else}}{{ toy.gcategory }}{{/if}}"
                                    required>
                            </div>
                            <div class="mb-3">
                                <label for="detail" class="form-label">Detail:</label>
                                <input type="text" id="detail" name="detail" class="form-control"
                                    value="{{#if toy.bdetail}}{{ toy.bdetail }}{{else}}{{ toy.gdetail }}{{/if}}"
                                    required>
                            </div>
                            <div class="mb-3">
                                <label for="price" class="form-label">Price:</label>
                                <input type="number" id="price" name="price" class="form-control"
                                    value="{{#if toy.bprice}}{{ toy.bprice }}{{else}}{{ toy.gprice }}{{/if}}"
                                    inputmode="numeric" required>
                            </div>
                            <div class="mb-3">
                                <label for="quantity" class="form-label">Quantity:</label>
                                <input type="number" id="quantity" name="quantity" class="form-control"
                                    value="{{#if toy.bquantity}}{{ toy.bquantity }}{{else}}{{ toy.gquantity }}{{/if}}"
                                    inputmode="numeric" required>
                            </div>
                            <div class="mb-3">
                                <label for="image" class="form-label">Image:</label>
                                <input type="text" id="image" name="image" class="form-control"
                                    value="{{#if toy.bimage}}{{ toy.bimage }}{{else}}{{ toy.gimage }}{{/if}}" required>
                            </div>
                            <button type="submit" class="btn btn-submit">Update Toy</button>
                            <a href="/admin/alltoys" class="btn btn-submit">Back to Admin Page</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>